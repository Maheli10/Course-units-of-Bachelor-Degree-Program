create table WORKER(WORKER_ID INT,
	fIRST_NAME VARCHAR (50),
	LAST_NAME VARCHAR(50),
	SALARY VARCHAR(10),
	jOINING_DATE DATE,
	DEPARTMENT VARCHAR(10));
	
SHOW TABLES;
	
DESC WORKER;

SELECT * FROM WORKER;
	
INSERT INTO WORKER(
		WORKER_ID,FIRST_NAME,LAST_NAME,SALARY,JOINING_DATE,DEPARTMENT)
	VALUES(
		001,'Monika','Arora','10000','20-02-2021' '09:00','HR'
	);
	
ALTER TABLE WORKER MODIFY COLUMN WORKER_ID VARCHAR(10);